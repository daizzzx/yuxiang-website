# 忽略清单：这些文件/文件夹不进 git 仓库（git 对它们视而不见）

# 依赖包：太大了，而且 package-lock.json 能让别人一键装出一模一样的
node_modules/

# 构建产物：dist/ 是流水线在服务器上重新构建的，本地的不用传
dist/

# 系统杂物
.DS_Store
Thumbs.db

# 本地环境变量（以后放密钥的地方，绝对不能上传）
*.local
.env
.env.*
